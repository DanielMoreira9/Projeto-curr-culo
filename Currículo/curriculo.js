const nota = mb;
if (nota === mb) {
    console.log ("é nóis prof.")
}
 else if (nota === b) {
    console.log ("pode ser mb né?")
 }
  else if (nota === r) {
    console.log ("prof. arredonda pra um b")
  } else {
    console.log ("tenha piedade")
  }